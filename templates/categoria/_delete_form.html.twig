<form method="post" action="{{ path('categoria_delete', {'id': categorium.id}) }}" onsubmit="return confirm('¿Quieres eliminar esta categoria?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorium.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> Eliminar categoria</button>
</form>
