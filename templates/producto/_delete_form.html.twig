<form method="post" action="{{ path('producto_delete', {'id': producto.id}) }}" onsubmit="return confirm('¿Quieres eliminar este chollo?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> Eliminar</button>
</form>
